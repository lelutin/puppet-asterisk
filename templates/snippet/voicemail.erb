[<%= @context %>]
<%= @name -%> => <%= @password.unwrap -%>,<%= @user_name -%>,<%= @email -%>,<%= @pager_email-%><% if @options.length > 0 -%>|<%= @options.keys.collect {|key| value = @options[key]; "#{key}=#{value}"}.join(",") -%><% end %>
