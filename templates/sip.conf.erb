; THIS FILE IS MANAGED BY PUPPET
; all local modifications will be lost
; To change values found in this file, see the $sip_general parameter of
; the asterisk module
;
; SIP Configuration for Asterisk
;
[general]
<% @sip_general.map do |name, value|
  if value.is_a? Array
    value.each do |v| -%>
<%= name -%>=<%= v %>
<%  end
  else -%>
<%= name -%>=<%= value %>
<% end
end -%>

#include </etc/asterisk/sip.registry.d/*.conf>
#include </etc/asterisk/sip.d/*.conf>
