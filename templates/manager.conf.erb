; THIS FILE IS MANAGED BY PUPPET
; all local modifications will be lost
; To change values found in this file, see the $manager_enable, $manager_port
; and $manager_bindaddr parameters of the asterisk module
;
; Asterisk Call Management support
;
[general]
enabled = <%= manager_enable %>
port = <%= manager_port %>
bindaddr = <%= manager_bindaddr %>

; No access is allowed by default.
; To set a password, create a file in /etc/asterisk/manager.d
#include </etc/asterisk/manager.d/*.conf>
