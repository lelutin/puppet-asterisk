; THIS FILE IS MANAGED BY PUPPET
; all local modifications will be lost
; To change values found in this file, see the $features_options and
; $features_featuremap parameters of the asterisk module
;
; Call Features (parking, transfer, etc) configuration
;
[general]
<% @features_general.sort.map do |key, value| -%>
<%= key %>=<%= value %>
<% end -%>

[featuremap]
<% scope.lookupvar("asterisk::features_featuremap").sort.map do |key, value| -%>
<%= key %> => <%= value %>
<% end -%>

[applicationmap]
<% scope.lookupvar("asterisk::features_applicationmap").sort.map do |key, value| -%>
<%= key %> => <%= value %>
<% end -%>

#include </etc/asterisk/features.d/*.conf>
