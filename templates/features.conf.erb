; THIS FILE IS MANAGED BY PUPPET
; all local modifications will be lost
; To change values found in this file, see the $features_general and
; $features_featuremap parameters of the asterisk module
;
; Call Features (parking, transfer, etc) configuration
;
[general]
<% @features_general.map do |key, value| -%>
<%= key %>=<%= value %>
<% end -%>

[featuremap]
<% scope.lookupvar("asterisk::features_featuremap").map do |key, value| -%>
<%= key %> => <%= value %>
<% end -%>

[applicationmap]
<% scope.lookupvar("asterisk::features_applicationmap").map do |key, value| -%>
<%= key %> => <%= value %>
<% end -%>

#include </etc/asterisk/features.d/*.conf>
