; THIS FILE IS MANAGED BY PUPPET
; all local modifications will be lost
; To change values found in this file, see the $voicemail_options parameter of
; the asterisk module
;
; Voicemail Configuration
;
[general]
<% voicemail_options.sort.map do |name, value|
  if value.is_a? Array
    value.each do |v| -%>
<%= name -%>=<%= v %>
<%  end
  else -%>
<%= name -%>=<%= value %>
<% end
end -%>

#include </etc/asterisk/voicemail.d/*.conf>
