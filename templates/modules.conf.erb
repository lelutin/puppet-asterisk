; THIS FILE IS MANAGED BY PUPPET
; all local modifications will be lost
; To change values found in this file, see the $modules_autoload,
; $modules_noload, $modules_load and $modules_global parameters of the
; asterisk module
;
; Asterisk Module Loader configuration file
;
[modules]
autoload=<%= @modules_autoload %>

<% scope.lookupvar("asterisk::modules_noload").each do |noload| -%>
noload => <%= noload %>
<% end -%>
<% scope.lookupvar("asterisk::modules_load").each do |load| -%>
load => <%= load %>
<% end -%>
[global]
<% scope.lookupvar("asterisk::modules_global").sort.map do |key, value| -%>
<%= key %>=<%= value %>
<% end -%>

