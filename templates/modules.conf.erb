; THIS FILE IS MANAGED BY PUPPET
; all local modifications will be lost
; To change values found in this file, see the $modules_autoload,
; $modules_noload, $modules_load and $modules_global_options parameters of the
; asterisk module
;
; Asterisk Module Loader configuration file
;
[modules]
autoload=<%= modules_autoload %>

<% modules_noload.each { |noload| -%>
noload => <%= noload %>
<% } %>
<% modules_load.each { |load| -%>
load => <%= load %>
<% } %>
[global]
<% modules_global_options.sort.map { |key, value| -%>
<%= key %>=<%= value %>
<% } -%>

