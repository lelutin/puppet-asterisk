; THIS FILE IS MANAGED BY PUPPET
; all local modifications will be lost
; To change values found in this file, see the $iax_general parameter of
; the asterisk module
;
; Inter-Asterisk eXchange driver definition
;
[general]
<% @iax_general.map do |name, value|
  if value.is_a? Array
    value.each do |v| -%>
<%= name -%>=<%= v %>
<%  end
  else -%>
<%= name -%>=<%= value %>
<% end
end -%>

#include </etc/asterisk/iax.registry.d/*.conf>
#include </etc/asterisk/iax.d/*.conf>
